\begin{frame}{Final Deliverables}
  \scriptsize
  \begin{itemize}
    \item End-to-end implementation for Kaggle training/evaluation diagnostics
    \item Quantitative results on unseen scenes across multiple view counts
    \item Qualitative 3D visualizations and failure-case analysis
    \item Final report discussing RGB-D source-depth strengths and limitations
    \item Reproducible Kaggle experiment scripts organized under
    \texttt{scripts/kaggle}
    \item Final validation rerun script with a P100-compatible Torch fallback
    for unstable Kaggle GPU allocation
    \item Submitted supervised-extension kernels for Run 12--18 and Phase 3
    label/subset kernels for Runs 19--20
    \item Submitted OARH v2 reconstruction path for Runs 21--23, exposing
    proxy-to-geometry transfer limits
    \item Submitted RSDH v2 reconstruction path for Runs 24--26, where proxy
    match validity fails the geometry gate
    \item Redesigned Run 27 as a reconstruction-aware ensemble; its gate selects
    all-candidate retention over learned filtering
    \item Added Run 28 source-ray correction to repair wrong depth without
    deleting valid occluded geometry
    \item Added Run 29 monodepth source-ray correction to approximate the Run 28
    oracle without GT depth at inference
    \item Added Run 30 RGB-D source-depth correction as the final accepted
    solution for the two remaining limits
    \item Added Run 31 as a frozen-policy coverage stress test over 360
    sparse-view groups; results are pending
    \item Added Run 32 as a direct RGB-D backprojection baseline without
    MV-DUSt3R+; comparison with Run 30 is pending
    \item Clean GitHub repository with curated docs, proposal sources, and PDF
    deck
  \end{itemize}
\end{frame}

\begin{frame}{Final Contributions}
  \footnotesize
  \begin{itemize}
    \item A strong RGB-only baseline that solves sparse-view instability through
    view selection and fixed confidence thresholds.
    \item A negative-analysis record showing why OARH/RSDH/RAJAH and monodepth
    should not be claimed as RGB-only solutions.
    \item A final RGB-D/source-depth method that passes reconstruction-level
    gates for overall quality, occlusion, and repeated/wrong-depth ambiguity.
    \item A clear limitation boundary: RGB-only is baseline/diagnostic; Run 30
    is the final source-depth contribution.
  \end{itemize}
\end{frame}
