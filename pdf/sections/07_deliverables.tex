\begin{frame}{Final Deliverables}
  \scriptsize
  \begin{itemize}
    \item Reproducible Kaggle experiment scripts under \texttt{scripts/kaggle}.
    \item Reconstruction metrics, hard-subset gates, and qualitative 3D
    visualizations.
    \item Run 11: strongest RGB-only sparse-view baseline.
    \item Runs 12--29: negative reconstruction evidence for learned
    RGB-only filtering/correction.
    \item Run 30: validated RGB-D/source-depth correction reference.
    \item Run 32: direct RGB-D backprojection diagnostic and circularity check.
    \item Run 33: clean MV-DUSt3R+ RGB-only candidate baseline.
    \item Run 34: side-by-side point clouds, dense GLBs, and normalized
    ND/DAc metrics.
    \item Run 37: fine-tuned estimated-depth source for the next correction run.
    \item Updated reports and slides for the RGB-only estimated-depth target
    setting.
  \end{itemize}
\end{frame}

\begin{frame}{Final Contributions}
  \footnotesize
  \begin{itemize}
    \item A strong RGB-only baseline that solves sparse-view instability through
    view selection and fixed confidence thresholds.
    \item A negative-analysis record showing why OARH/RSDH/RAJAH and monodepth
    should not be claimed as RGB-only solutions.
    \item A validated RGB-D/source-depth reference that passes reconstruction
    gates for overall quality, occlusion, and repeated/wrong-depth ambiguity.
    \item A target RGB-only estimated-depth setting: MV-DUSt3R+ candidates are
    corrected using depth predicted by the fine-tuned depth model.
    \item A Run 33 RGB-only backbone baseline confirming that Run 30's gain is
    not just MV-DUSt3R+ candidate retention.
    \item A clear limitation boundary: Run 30 uses true source depth; the
    estimated-depth target needs separate reconstruction-gate evidence.
  \end{itemize}
\end{frame}
